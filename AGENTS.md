# Gmail MCP Agent Instructions

Always read and follow `PROJECT_RULES.md` when working in this repository.

When the user says `update_mdfiles`, read and follow `MAINTENANCE_RULES.md`.

Longer public-facing agent context files live in `docs/md_files/`.
